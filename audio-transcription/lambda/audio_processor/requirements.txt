# Lambda function-specific dependencies (minimal for Phase 4)
# Emotion detection and audio quality features temporarily disabled

# AWS SDK
boto3>=1.28.0
botocore>=1.31.0
amazon-transcribe>=0.6.0

# Async support
asyncio>=3.4.3

# Audio processing (essential only)
numpy==1.24.4

# Text similarity (for shared module)
python-Levenshtein>=0.21.0

# JWT token validation (for shared module)
PyJWT>=2.8.0
cryptography>=41.0.0

# HTTP client (for shared module)
requests>=2.31.0

# NOTE: Temporarily removed large dependencies (exceed Lambda 250MB limit):
# - scipy>=1.11.0 (40MB+)
# - librosa>=0.10.0 (60MB+)
# - soundfile>=0.12.0 (5MB)
# These are needed for emotion_dynamics and audio_quality modules.
# See OPTIONAL_FEATURES_REINTEGRATION_PLAN.md for adding them back.
